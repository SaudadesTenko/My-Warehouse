~~~c
//判断一个数是否为素数
#include<stdio.h>
int main()
{
	int n,i;
	printf("请输入一个整数:n=");
	scanf("%d",&n);
	for(i=2;i<n;i++)
	{	if(n%i==0)
			break;		//这是一个巧妙的退出点
	}
	if(i<n)
		printf("%d不是素数.\n",n);
	else
		printf("%d是素数.\n",n);

	
	return 0;
} 
